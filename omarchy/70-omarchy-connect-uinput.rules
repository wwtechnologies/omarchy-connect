# Omarchy Connect injects the remote keyboard and mouse through uinput.
# uaccess grants the user logged in at the seat access to /dev/uinput.
KERNEL=="uinput", SUBSYSTEM=="misc", TAG+="uaccess", OPTIONS+="static_node=uinput"
